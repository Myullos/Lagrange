# Curvature-Constrained Lagrange Interpolation

## 概要（Abstract）

本研究では、**ラグランジュ補間（Lagrange Interpolation）**における数値的不安定性、特に**ルンゲ現象（Runge phenomenon）**の抑制を目的として、補間手法の再構築を行った。対象関数としては代表的な試験関数である
f(x) = 1 / (1 + x²) を用いた。

まず、補間点として**チェビシェフノード（Chebyshev nodes）**を採用し、大域的な振動の抑制を図った。さらに、補間誤差を区間ごとに評価し、**積分誤差（面積誤差）に基づく局所的再補間（re-interpolation）**を導入することで、誤差の大きい区間に対して重点的な補正を行った。

次に、補間精度の向上のために、ノードの**y値そのものを最適化する手法**を導入した。具体的には、補間関数と真の関数との**二乗誤差積分を最小化するようにランダム探索による調整**を行い、従来の補間よりも高精度な近似を実現した。

さらに、補間関数の滑らかさに着目し、**曲率（2階微分）に基づく補正手法**を提案した。補間関数 P(x) に対し、P''(x) を用いた補正項を加え、その係数を誤差最小化により決定することで、**振動を抑えつつ精度を向上させる枠組み**を構築した。

加えて、局所的な自由度を導入するために、区間ごとに定義される**バブル関数（bubble function）**を用いた補間を提案した。この関数は**補間点（節点）では常に0となり、節点間の内部でのみ値を持つ**という性質を持つため、補間条件を保持したまま局所的な形状制御を可能にする。本研究では、このバブル関数を用いて補間関数に局所補正項を加え、**節点を固定したまま区間内部のみを調整する手法**を構築した。さらに、この補正に対して**曲率の二乗積分（∫(f''(x))² dx）を最小化する最適化問題**を設定することで、不要な振動を抑制しつつ滑らかな補間を実現した。

最後に、節点における傾きを変数とする**反復的傾き最適化手法（iterated slope optimization）**を構築した。この手法では、補間関数の導関数と節点傾きを自己一致させる反復計算を行うことで、**曲率を制御しつつ高精度な補間**を実現した。

数値実験の結果、これらの手法はいずれも従来のラグランジュ補間と比較して、**誤差の低減および振動の抑制に有効である**ことが確認された。特に、曲率に基づく最適化および局所補正を組み合わせた手法は、**高い安定性と精度を両立する補間法**であることが示された。

本研究は、ラグランジュ補間を**「単なる補間手法」から「最適化問題として再定式化する枠組み」へ拡張**するものである。今後は理論的解析の深化および高次元問題への応用を進め、**大学・大学院における研究へと発展させることを目指す。**

---

## 背景

高次多項式補間は理論的には任意の連続関数を近似可能であるが、実際には端点付近で大きな振動（ルンゲ現象）が発生しやすい。この問題は数値解析・科学計算において古典的かつ重要な課題である。

従来はノードの選択（例：チェビシェフノード）による改善が主であったが、本研究ではさらに一歩進め、**補間そのものを最適化問題として捉える**というアプローチを採用した。

---

## 提案手法（Overview）

本研究では以下のアプローチを段階的に導入した：

* チェビシェフノードによる振動抑制
* 積分誤差に基づく局所再補間
* ノード値（y値）の最適化
* 曲率（2階微分）に基づく補正
* バブル関数による局所自由度の導入
* 曲率最小化問題としての定式化
* 傾き最適化に基づく反復手法

---

## 本研究の特徴

* **補間条件を保ったまま最適化が可能**
* **局所補正と大域的安定性の両立**
* **曲率最小化による滑らかさ制御**
* **補間問題の最適化問題への拡張**

---

## 今後の展望

* 理論的収束性・安定性の解析
* 最適化アルゴリズムの高度化
* 高次元・多変数補間への拡張
* 他の関数近似手法との統合

本研究を基盤として、今後はより体系的な理論構築を行い、**大学・大学院での研究へと発展させていくことを目指す。**
